{
  "name": "my-netlify-project",
  "version": "1.0.0",
  "dependencies": {
    "node-fetch": "^2.6.x",
    "@contentful/rich-text-html-renderer": "^15.16.0"
  },
  "scripts": {
    "build": "echo 'No build step required' && exit 0"
  }
}
